# Quelle fuer providers/njalla.yaml - von Hand gepflegt.
# Nach jeder Aenderung: python3 tools/manifest_merge.py
name: Njalla
description: Datenschutzorientierter Registrar. Der Schlüssel gilt je Eintrag, nicht je Konto.
website: https://njal.la
categories:
- registrar
- dns_provider
engine:
  adapter: native
  protocol: http
capabilities:
  ipv4: true
  ipv6: true
  ipv6_suffix: true
  ttl: false
  proxy: false
request:
  url: https://njal.la/update/
  params:
    h: '{hostname}'
    k: '{key}'
    a: '{ipv4}'
    aaaa: '{ipv6}'
  headers:
    Accept: application/json
  success:
    json_path_equals:
      message: record updated
fields:
- id: key
  type: secret
  label: Eintrags-Schlüssel
  required: true
  help: 'Gilt für einen einzelnen Dynamic-Eintrag, nicht für das ganze Konto. Bei Njalla die Domain öffnen
    → Add record → Typ "Dynamic" → Subdomain eintragen; der Schlüssel steht danach im angelegten Eintrag.

    '
