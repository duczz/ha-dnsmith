# Quelle fuer providers/hostinger.yaml - von Hand gepflegt.
# Nach jeder Aenderung: python3 tools/manifest_merge.py
name: Hostinger
description: Hoster mit eigener API. Der Token wird im hPanel erzeugt.
website: https://www.hostinger.com
categories:
- dns_provider
engine:
  adapter: native
  protocol: http
capabilities:
  ipv4: true
  ipv6: true
  ipv6_suffix: true
  ttl: true
  proxy: false
request:
  url: https://developers.hostinger.com/api/dns/v1/zones/{domain}
  method: PUT
  auth:
    mode: bearer
    format: '{token}'
  headers:
    Content-Type: application/json
    Accept: application/json
  body_type: json
  body:
    overwrite: true
    zone:
    - name: '{owner}'
      type: '{rrtype}'
      ttl: '{ttl}'
      records:
      - content: '{ip}'
  success:
    status:
    - 200
fields:
- id: token
  type: secret
  label: API-Token
  required: true
  help: Nicht dein hPanel-Passwort. Im hPanel unter Profil → API erzeugen.
- id: ttl
  type: integer
  label: TTL
  help: Sekunden, optional. Ohne Angabe verwendet Hostinger 14400.
example:
  ttl: '300'
