# Quelle fuer providers/opendns.yaml - von Hand gepflegt.
# Nach jeder Aenderung: python3 tools/manifest_merge.py
name: OpenDNS
description: DNS-Dienst von Cisco. Aktualisiert die dynamische Adresse eines im Dashboard angelegten Netzwerks.
website: https://www.opendns.com
categories:
- free_ddns
- dyndns2_compatible
engine:
  adapter: native
  protocol: dyndns2
capabilities:
  ipv4: true
  ipv6: true
  ipv6_suffix: true
  ttl: false
  proxy: false
request:
  url: https://updates.opendns.com/nic/update
  auth:
    mode: basic
    username: '{username}'
    password: '{password}'
  params:
    hostname: '{hostname}'
    myip: '{ip}'
  success:
    vocabulary: dyndns2
fields:
- id: username
  type: text
  label: Benutzername
  required: true
  help: Dein Login für das OpenDNS- bzw. Umbrella-Dashboard, meist die E-Mail-Adresse.
- id: password
  type: secret
  label: Konto-Passwort
  required: true
  help: 'Das Passwort dieses Kontos; ein eigenes Update-Passwort gibt es nicht. Das Netzwerk im Dashboard
    muss auf dynamische IP gestellt sein.

    '
notes:
- Was hier als Domain eingetragen wird, ist der Name des Netzwerks aus dem OpenDNS-Dashboard — kein DNS-Eintrag.
  Er lässt sich nirgends auflösen. DNSmith prüft bei diesem Anbieter deshalb nicht über DNS, ob die Adresse
  schon stimmt, sondern vergleicht mit der zuletzt gesendeten.
