# Quelle fuer providers/goip.yaml - von Hand gepflegt.
# Nach jeder Aenderung: python3 tools/manifest_merge.py
name: GoIP.de
description: Kostenloser deutscher DDNS-Dienst. Die Zugangsdaten werden je Router vergeben, nicht je Konto.
website: https://www.goip.de
categories:
- german
- free_ddns
- dyndns2_compatible
engine:
  adapter: native
  protocol: http
capabilities:
  ipv4: true
  ipv6: true
  ipv6_suffix: true
  ttl: false
  proxy: false
request:
  url: https://www.goip.de/setip
  auth:
    mode: basic
    username: '{username}'
    password: '{password}'
  params:
    subdomain: '{hostname}'
    username: '{username}'
    password: '{password}'
    shortResponse: 'true'
    ip: '{ipv4}'
    ip6: '{ipv6}'
  success:
    status:
    - 200
  failures:
    zugriff verweigert: auth
fields:
- id: username
  type: text
  label: Router-Benutzername
  required: true
  help: 'Nicht dein Webzugang-Login. GoIP erzeugt je Router eigene Zugangsdaten; nur die funktionieren
    in der Update-URL. Sie stehen in der Routerliste unter goip.de.

    '
- id: password
  type: secret
  label: Router-Passwort
  required: true
  help: 'Das Passwort zum Router-Benutzernamen, nicht das des Administrationsbereichs. Ebenfalls in der
    Routerliste, dort über das Augensymbol sichtbar.

    '
