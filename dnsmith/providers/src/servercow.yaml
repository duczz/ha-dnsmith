# Quelle fuer providers/servercow.yaml - von Hand gepflegt.
# Nach jeder Aenderung: python3 tools/manifest_merge.py
name: Servercow
description: Deutscher Hoster mit eigener DNS-API, deren Zugänge getrennt angelegt werden.
website: https://www.servercow.de
categories:
- german
- dns_provider
engine:
  adapter: native
  protocol: http
capabilities:
  ipv4: true
  ipv6: true
  ipv6_suffix: true
  ttl: true
  proxy: false
request:
  url: https://api.servercow.de/dns/v1/domains/{domain}
  method: POST
  auth:
    mode: header
    header: X-Auth-Username
    format: '{username}'
  headers:
    Content-Type: application/json
    Accept: application/json
    X-Auth-Password: '{password}'
  body_type: json
  body:
    type: '{rrtype}'
    name: '{subdomain}'
    content: '{ip}'
    ttl: '{ttl}'
  success:
    status:
    - 200
fields:
- id: username
  type: text
  label: DNS-API-Benutzername
  required: true
  help: 'Nicht dein Kundenkonto-Login, sondern der Name eines DNS-API-Zugangs. Im Servercow-Panel oben
    rechts über deinen Kundennamen → "Servercow DNS-API" → "Neuen API-Zugang erstellen".

    '
- id: password
  type: secret
  label: DNS-API-Passwort
  required: true
  help: 'Wird beim Erstellen des Zugangs nur einmal angezeigt und ist später nicht mehr auslesbar — dann
    hilft nur ein neuer API-Zugang.

    '
- id: ttl
  type: integer
  label: TTL
example:
  ttl: '600'
  username: servercow_username
