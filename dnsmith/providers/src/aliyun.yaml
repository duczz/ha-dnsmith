# Quelle fuer providers/aliyun.yaml - von Hand gepflegt.
# Nach jeder Aenderung: python3 tools/manifest_merge.py
name: Aliyun
description: Chinesischer Cloud-Anbieter. DNS-Einträge werden über die Alibaba-Cloud-API geändert.
website: https://www.aliyun.com
categories:
- cloud_dns
engine:
  adapter: python
  module: aliyun
capabilities:
  ipv4: true
  ipv6: true
  ipv6_suffix: true
  ttl: false
  proxy: false
fields:
- id: access_key_id
  type: text
  label: AccessKey ID
  required: true
  help: 'Der öffentliche Teil eines AccessKey-Paares. In der RAM-Konsole unter Identities → Users → Benutzer
    → Credentials → Create AccessKey anlegen. Nimm einen RAM-Benutzer, nicht das Hauptkonto.

    '
- id: access_secret
  type: secret
  label: AccessKey Secret
  required: true
  help: 'Der geheime Teil desselben Paares, nicht dein Konto-Passwort. Wird nur einmal direkt nach dem
    Anlegen angezeigt.

    '
- id: region
  type: text
  label: Region
  placeholder: cn-hangzhou
  help: Leer lassen verwendet cn-hangzhou. Nur ändern, wenn die Zone woanders liegt.
