# Quelle fuer providers/domeneshop.yaml - von Hand gepflegt.
# Nach jeder Aenderung: python3 tools/manifest_merge.py
name: Domeneshop
description: Norwegischer Registrar mit eigener API aus Token und Secret.
website: https://domene.shop
categories:
- registrar
engine:
  adapter: native
  protocol: dyndns2
capabilities:
  ipv4: true
  ipv6: true
  ipv6_suffix: true
  ttl: false
  proxy: false
request:
  url: https://api.domeneshop.no/v0/dyndns/update
  auth:
    mode: basic
    username: '{token}'
    password: '{secret}'
  params:
    hostname: '{hostname}'
    myip: '{ip}'
  success:
    status:
    - 200
    - 204
fields:
- id: token
  type: secret
  label: API-Token
  required: true
  help: Nach dem Login im Kontrollpanel unter API erzeugen. Nicht dein Login-Name.
- id: secret
  type: secret
  label: API-Secret
  required: true
  help: 'Entsteht im selben Schritt wie der Token und wird nur dort einmal angezeigt. Nicht dein Konto-Passwort.

    '
